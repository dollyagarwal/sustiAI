"""Top-level source package marker for tests and imports.

This allows tests to import using `src.sustiai` (keeps compatibility with the
project's existing imports). This file intentionally contains no runtime
logic.
"""
